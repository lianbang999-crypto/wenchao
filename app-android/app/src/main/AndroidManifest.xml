<?xml version="1.0" encoding="utf-8"?>
<!--
    印祖文钞 · 离线阅读应用

    原为 bubblewrap 生成的 TWA 清单，需要 asset_statements、DelegationService、
    WebViewFallbackActivity 等一整套「委托浏览器打开网站」的组件。改为自建 WebView
    后内容随包出厂，那些全部不再需要，已一并移除。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 只用于检查更新与下载新版；正文阅读全程不联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 「有新版直接更新」要唤起系统安装器 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/launcherName"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 下载好的新版安装包要经 FileProvider 交给系统安装器，不能直接递 file:// -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>
    </application>
</manifest>
