# 全站安全响应头。站点纯静态、无 eval、无外域请求（AI 走同源 /api/ai），
# 故 connect-src/font-src/object-src 等一律收到最紧。
#
# script-src 为何留 'unsafe-inline'：每页两段 <script type="application/ld+json">
# 结构化数据（Article/BreadcrumbList/Book 图谱）会被严格 script-src 判为内联脚本拦下，
# 每页刷两条控制台报错。它们各页内容不同，2565 页没法逐个下 hash。
# 站内没有可执行的内联脚本，AI 回答经 ai-core.js 的 esc() 转义后才入 DOM，
# 没有 HTML 注入面——用整套 SEO 结构化数据换这一条的严格度不划算。
#
# 注意：若把 config.js 的 aiEndpoint 改成跨域 workers.dev 地址，须同步放开 connect-src。
/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  X-Frame-Options: SAMEORIGIN
  Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=()
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://static.cloudflareinsights.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; media-src 'self' blob:; font-src 'self'; connect-src 'self' https://cloudflareinsights.com; base-uri 'self'; form-action 'self'; frame-ancestors 'self'; object-src 'none'

/config.js
  Cache-Control: no-store

/index.html
  Cache-Control: no-cache

/
  Cache-Control: no-cache

/a/*
  Cache-Control: no-cache

/v/*
  Cache-Control: no-cache

/t/*
  Cache-Control: no-cache

/ying/
  Cache-Control: no-cache

/ask/
  Cache-Control: no-cache

/img/*
  Cache-Control: public, max-age=2592000, immutable

# 目录与篇 JSON 随内容重建而变，且前端是按 no-cache 取的：显式声明一下，
# 保证浏览器留着副本走 ETag 协商（304），而不是落到启发式缓存或每次全量重下。
/data/*
  Cache-Control: no-cache

# css/字体全部经 ?v= 版本串引用（构建脚本统一注入），内容变则版本变，可放心长缓存。
# 字体切片文件名即内容标识（切片命名稳定），同样不可变。
/css/*
  Cache-Control: public, max-age=31536000, immutable

/font/*
  Cache-Control: public, max-age=31536000, immutable

# js 逐个声明，不用 /js/* 通配——这是踩过坑之后的写法，别图省事改回通配。
#
# 2026-08-29 实测：Cloudflare Pages 的 _headers 里，通配规则会盖过同目录下的
# 精确路径规则，且与书写顺序无关（把 /js/ai-core.js 挪到 /js/* 前面同样无效，
# 线上照旧返回 immutable）。而 ai-core.js 是被 app.js 以裸 import './ai-core.js'
# 引入的、没有版本串——一旦按不可变缓存一年，往后改它就送不到已装用户手上，
# 得等一年缓存过期。新增 js 文件时记得在这里补一行。
/js/ai-core.js
  Cache-Control: no-cache

/js/qrcode.js
  Cache-Control: public, max-age=2592000

/js/opencc.js
  Cache-Control: public, max-age=2592000

/js/app.js
  Cache-Control: public, max-age=31536000, immutable

/js/ask.js
  Cache-Control: public, max-age=31536000, immutable

/js/pwa.js
  Cache-Control: public, max-age=31536000, immutable

/js/share.js
  Cache-Control: public, max-age=31536000, immutable

/js/offline.js
  Cache-Control: public, max-age=31536000, immutable

# SW 脚本本体：绝不能被长缓存，否则修好的版本送不到已装用户手上。
# ⚠️ 2026-08-29 实测：线上这条没生效，/sw.js 实际返回 max-age=2678400（31 天），
# 而这个值并不在本文件任何一条规则里——应是 Cloudflare 面板的
# 「Browser Cache TTL」（默认 1 个月）覆盖了它。若要根治，去
# Cloudflare → Caching → Configuration，把 Browser Cache TTL 改为
# 「Respect Existing Headers」。
# 影响有限但不为零：按 Service Worker 规范，浏览器检查 SW 主脚本更新时会绕过
# HTTP 缓存（updateViaCache 默认 imports），所以更新照常能发现；
# 这里收紧只是不把安全绳系在「各家 WebView 都严格守规范」这个假设上。
/sw.js
  Cache-Control: no-store, no-cache, must-revalidate, max-age=0

/sitemap.xml
  Cache-Control: no-cache

/robots.txt
  Cache-Control: no-cache

# TWA 数字资产链接：Chrome 安装/打开 APP 时校验，须始终最新（Play 上架后要补 Play 签名指纹）
/.well-known/assetlinks.json
  Cache-Control: no-cache
  Content-Type: application/json

# 启动计数像素：恒不缓存，每次启动都要真的落到服务器日志里才数得到
/i/*
  Cache-Control: no-store, no-cache, must-revalidate
  Content-Type: image/gif

# 安卓安装包：文件名带版本号即内容标识，可长缓存；显式声明 MIME 与下载文件名，
# 免得个别浏览器把它当未知类型在页内打开或存成 .zip
/app/*.apk
  Content-Type: application/vnd.android.package-archive
  Content-Disposition: attachment
  Cache-Control: public, max-age=31536000, immutable
